# Database Integration

Canonical documentation: [`../../../docs/11-database.md`](../../../docs/11-database.md).

Implemented: PostgreSQL repository/service, read-only transaction, SQL token validation, table/column allowlists, result/time limits and safe transient retry.

Partial: no live database is wired into the default API composition; no application schema is required by this repository.
